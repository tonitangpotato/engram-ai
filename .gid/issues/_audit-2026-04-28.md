# Engram Open-Issue Audit — 2026-04-28

> Snapshot of engram repository's open / in_progress issues + v0.3 in_progress features at the end of the 2026-04-28 overnight autopilot session. Built so potato can scan once at wake-up and pick the next direction without rereading every issue.md.
>
> All status fields verified by `grep` against the actual `issue.md` files (frontmatter format for ISS-040..044, inline `**Status**` format for ISS-045+). All titles quoted from the issue.md `Title` line / H1.

---

## 1. Open / in_progress Issues

### Active (need real work)

- **ISS-040** [P2, open] — *"Memory::graph(&self) requires SqliteGraphStore<C: Borrow<Connection>> generic refactor"*
  - Component: `crates/engramai/src/graph/store.rs`
  - Related: `v03-graph-layer`
  - Blocks the canonical `Memory::graph()` borrow-based read API specified in v03-graph-layer design §5.

- **ISS-041** [P1, open] — *"Episode struct definition (v0.3 ingestion contract)"*
  - Component: `crates/engramai/src/resolution/`
  - Related: `v03-resolution`, `v03-graph-layer`
  - Defines the v0.3 ingestion input contract (`Memory::add_episode(ep: Episode)`). Currently episodes only exist as bare UUIDs.

- **ISS-042** [P1, open] — *"ReextractReport struct definition (v0.3 retry surface)"*
  - Component: `crates/engramai/src/resolution/`
  - Related: `v03-resolution`, `v03-graph-layer`
  - Defines the return contract for `Memory::reextract_episodes`. Struct not yet declared anywhere.

- **ISS-043** [P2, open] — *"Restore literal single-tx atomicity in PipelineRecordProcessor (T9)"*
  - Component: `crates/engramai-migrate/src/processor.rs`
  - Related: `v03-migration`, `v03-graph-layer`
  - Severity: low — semantic equivalence already achieved in T9 (commit `e458540`). Doc/contract alignment with design §5.2.

- **ISS-044** [P1, open] — *"Wire MigrationOrchestrator::run_backfill to PipelineRecordProcessor"*
  - Component: `crates/engramai-migrate/src/cli.rs`
  - Related: `v03-migration`, `ISS-043`
  - Blocks any end-to-end v0.3 retrieval test on real migrated data; `engram migrate --accept-forward-only` currently bails with `phase4: backfill not yet implemented` despite the processor existing.

- **ISS-048** [P0, in_progress] — *"EntityExtractor / EdgeExtractor architectural mismatch — pattern-based entities cannot keep up with LLM-extracted edge subjects/objects, causing 100% `unresolved_subject` failure on fresh-ingest"*
  - Tonight: Option B (lift novel edge endpoints) core implementation + 2 unit tests landed in commit `38be875`.
  - Status intentionally left `in_progress` — LoCoMo conv-26 acceptance gate still **deferred** (not run tonight). issue.md "Acceptance criteria" LoCoMo block is the remaining gate.

### Closed / done this session (housekeeping)

- **ISS-045** [closed-superseded, 2026-04-27] — *"Fresh-ingest path does not write v0.3 graph layer"* — initial diagnosis incorrect; superseded by ISS-046.
- **ISS-046** [closed, 2026-04-28, commit `de9a270`] — *"engram store CLI does not install pipeline pool — v0.3 graph stays empty on fresh ingest"* — fixed in `950159d`.
- **ISS-047** [done, 2026-04-28, commit `66097cc`] — *"closed-set failure-label allowlist mismatches pipeline producers"* — verified on commit `be35217` (32/32 LoCoMo conv-26 fresh-ingest, no rollback). Forward-linked to ISS-048.

---

## 2. v0.3 in_progress Features (from `.gid-v03-context/graph.db`)

- **feature:v03-retrieval** — *"Engram v0.3 Retrieval — Query-time path"* (parent feature, in_progress).
- **feature:retrieval-classification** — *"Intent classification & routing (§3, §4.7)"* (in_progress).
- **feature:retrieval-execution** — *"Plan execution + fusion + observability (§4–§8)"* (in_progress).
- **iss-018** — *"ISS-018: Recall Intent Classification"* (in_progress, mirror node tracking ISS-018 in v0.3 graph).
- **iss-021** — *"ISS-021: Subdim Extraction Coverage"* (in_progress, mirror node tracking ISS-021).

These three feature nodes cluster around the retrieval orchestrator path — the ingest side has working v0.3 wiring (ISS-046/047) and a partial fix in flight (ISS-048), so the natural next push is making retrieval actually use what gets written.

---

## 3. Critical Path (dependency-driven)

Three independent chains; potato picks one per session, not all at once.

### Chain A — retrieval correctness (sits on ISS-048)

```
ISS-048 (in_progress, lift novel endpoints)
  └─ LoCoMo conv-26 acceptance run (deferred from tonight)
       └─ ISS-021 (subdim extraction coverage) — needs clean retrieval baseline to measure
       └─ ISS-018 (recall intent classification) — needs entities/edges actually present
```

ISS-048 must finish its LoCoMo gate before ISS-021 / ISS-018 can be cleanly tested. Without entities + edges in the graph, intent classification and subdim coverage measurements run on empty data.

### Chain B — v0.3 ingestion contract (blocks retrieval orchestrator wiring)

```
ISS-041 (Episode struct) ─┐
ISS-042 (ReextractReport) ─┴─→ Memory::add_episode + reextract_episodes signatures wireable
                              └─→ retrieval orchestrator can call into a stable contract
```

Both are P1 because every higher-level v0.3 surface (`add_episode`, `reextract_episodes`, retrieval) currently dangles on missing struct definitions. Two independent struct-definition issues — can be done same session.

### Chain C — migration block (independent, doesn't block retrieval)

```
ISS-044 (wire run_backfill → PipelineRecordProcessor)
  ├─ ISS-043 (literal single-tx atomicity restoration) — doc/contract alignment
  └─ unblocks: end-to-end v0.3 migration on existing v0.2 DBs
```

ISS-040 (graph borrow-based API refactor) is P2 cleanup — improves API ergonomics but doesn't gate any of the above chains. Defer unless touching `graph/store.rs` for an adjacent reason.

---

## 4. Suggested Next-Session Priority

Pick **one** of:

1. **Finish ISS-048** — run LoCoMo conv-26 acceptance with the lift implementation from commit `38be875`. Confirm entities + edges land. If green → close ISS-048 and unblocks ISS-021 / ISS-018 measurement work. Highest leverage if it works on first run; lower leverage if a second extractor bug surfaces.
2. **Unblock retrieval orchestrator wiring** — write ISS-041 (Episode) + ISS-042 (ReextractReport) struct definitions in one focused session. Both are spec-out-of-doc-into-code tasks; small surface, both P1, opens up the v03-retrieval feature work in `.gid-v03-context/graph.db`.
3. **Migration block** — ISS-044 (wire backfill) + ISS-043 (atomicity doc) lets `engram migrate` actually run end-to-end on existing v0.2 DBs. Independent of retrieval; safe to do in parallel context.

### Recommended

**Option 1 (ISS-048 LoCoMo verification)** — lowest commit-to-value latency: tonight's commit either passes or surfaces the next concrete bug. Either outcome moves the project forward more than starting a fresh chain. Pair with a 5-minute look at the LoCoMo run logs before deciding whether to keep digging or pivot to Chain B.

If LoCoMo passes cleanly → switch to Chain B (ISS-041 + ISS-042) for the rest of the session, since retrieval orchestrator wiring is the next real blocker.

If LoCoMo fails → file a follow-up issue with the actual symptom and **stop** rather than chase it for hours; that's a fresh-eyes problem, not an autopilot problem.
